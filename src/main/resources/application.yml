server:
  port: 8080

spring:
  main:
    web-application-type: servlet
  application:
    name: transaction-service
  data:
    mongodb:
      uri: mongodb://root:secret@localhost:27017/booking_db?authSource=admin

serviex:
  base-url: http://localhost:9090
  transactions-path: /getTrx

jwt:
  secret: local-dev-secret-change-me-1234567890

error:
  mapping:
    code: SERVIEX_CONFLICT
    error: 500

booking:
  api:
    url: https://restful-booker.herokuapp.com/booking/{id}
  error:
    message: ketoKet

